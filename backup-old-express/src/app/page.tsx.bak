'use client';

export default function App() {
  return (
    <div className="p-10">
      <h1 className="text-5xl font-bold text-green-600 mb-8">
        🍺 Grok Pub Accounting - Working
      </h1>
      
      <div className="space-y-6 max-w-md">
        <button 
          onClick={() => alert('Transactions page would open here')}
          className="w-full bg-blue-600 text-white py-6 rounded-2xl text-2xl font-medium"
        >
          📋 Transactions
        </button>

        <button 
          onClick={() => alert('Rules page would open here')}
          className="w-full bg-gray-800 text-white py-6 rounded-2xl text-2xl font-medium"
        >
          ⚙️ Bank Rules
        </button>
      </div>
    </div>
  );
}