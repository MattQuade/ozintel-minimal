.vercel
node_modules
.next
.env
.env.local
.env.*.local

# Capacitor / native build artifacts
android/.gradle
android/app/build
android/build
android/captures
android/local.properties
ios/App/Pods
ios/App/build
ios/DerivedData
*.keystore
*.jks
tsconfig.tsbuildinfo
# Tesseract.js may drop a cache copy in the app cwd on Render; keep vendored data.
/eng.traineddata

# Per-account Accounting/Ops PIN hashes (live on OZINTEL_DATA_DIR in production)
data/accounting-pins.json
